---
layout: layouts/base.njk
bodyClass: body-post
---

<div class="post">
  <div id="post-header" class="post-header">
    <h2 class="post-title">{{ title }}</h2>
    <em>
      <time class="post-date" datetime="{{ date | date('dd/MM/yyyy') }}">
        {{ date | date('dd/MM/yyyy') }}
      </time>
      {%- if location -%}
      📍 {{ location }}
      {%- endif -%}
    </em>
    {%- if topic -%}
    <span style="float: right"><em>{{ topic }}</em></span>
    {%- endif -%}
  </div>
  <hr/>
  <div class="post-body">
    {{ content | safe }}
  </div>
  <div class="post-footer">
    <a href="#post-header">Back to top</a>
  </div>
</div>
